# NexusOps — Task Board

Board linking every feature spec + task file. Global contracts live in `specs/requirements.md` + `specs/design.md`; per-feature spec + tasks live under `specs/features/<name>/`. Shared contracts are referenced, never duplicated, in feature files.

**Build order:** 1 → 2 → 3 → 4 → 5 → 6 → 7 (later features depend on earlier ones).

| # | Feature | Spec | Tasks | State |
|---|---|---|---|---|
| 1 | Webhook ingestion | `features/webhook-ingest/spec.md` | `features/webhook-ingest/tasks.md` | **done** |
| 2 | MCP server | `features/mcp-server/spec.md` | `features/mcp-server/tasks.md` | **done** (Phases 1–5 ✅) |
| 3 | State machine | `features/state-machine/spec.md` | `features/state-machine/tasks.md` | **done** (Phases 1–4 ✅; Phase 5 partial-pass recorded) |
| 4 | Streaming dashboard | `features/streaming-dashboard/spec.md` | `features/streaming-dashboard/tasks.md` | **done** (Phases 1–4 ✅; Phase 5 skipped-by-user, recorded) |
| 5 | Benchmark harness | `features/benchmark/spec.md` | `features/benchmark/tasks.md` | **done** (Phases 1–4 ✅; Phase 5 incorrect-answer → 2 fixes + tests, recorded; Phase 4f OTel GenAI tracing/eval per D-7; Phase 4g root-caused the 8.6% run — 3 fixes; Phase 4h second live validation — plan fix verified, judge-outage guardrail; Phase 4i stratified quick-run `--limit N`; Phase 4j first clean verdict via `--limit 10` — severity 7/10, root cause 2/10, gate 2/10 = requires_approval contract bug; Phase 4k Basket-1 fixes — NG-1 gate lock, outage separation, evidence-forced RCA, behavior-based judge; Phase 4l re-record blocked by `429 free-models-per-day` 50-call cap (resets 05:00 PKT) — 7 outages, 3 answered, judge ungradable today; Phase 4m provider switch → Gemini per D-9 (config-only), first live record **8/10 answered** (best ever), judge quota-burned by retry×20/day — fixed via `_quota_exhausted` non-retryable + retries capped at 2 + probe-guarded `retry_judge.sh`; clean verdict pending frontier quota reset (midnight Pacific): `bash scripts/retry_judge.sh outG1.json`; 67 tests, 2026-09-22) |
| 6 | Demonstration film | `features/demo-film/spec.md` | `features/demo-film/tasks.md` | **done** (D-10 approved 2026-09-23; Pattern B player over real checkpoint + live §5.4 gate driving the production approve→rollback chain; real scoped GitHub tag rollback — stdlib, 0 new deps, Context7-stamped; 76 tests; browser-verified c-02 live gate + c-01 honest outage) |
| 7 | Live operations console | `features/live-console/spec.md` | `features/live-console/tasks.md` | **in-progress** (D-11 approved 2026-09-23: React/Vite/Tailwind console chosen by user + true-live real-model cycles; pipeline voice via driver-level `astream` — LangGraph 1.2.11 probe-verified; GateAwaiter human §5.4 seam; serve process composes webhook+worker+WS+`/film`+static; FR-10 + §5.5 added; film kept as history tab; T-7.1–7.4 P1/P2 done, P3 in flight) |
**in progress 2026-09-23:** 4m DoD resumed on reset quota — first probe failed and exposed a blind spot now fixed: probe required truthy `content` (healthy `finish_reason=length` responses have none) and mishandled Gemini's array-form `[{error}]` bodies; both `retry_judge.sh`/`retry_record.sh` probes now accept any `choices` array, fail only on truthy `error`, verified offline vs 5 captured shapes. Replay running (window flaky at 00:09 PT, 503s interleaved with healthy probes, probe-guard sleeping between tries); DoD: `graded ≥ 8`, `judge_failures 0`, `inconclusive false`. |

Update a feature's state here whenever its `tasks.md` changes (AGENTS.md §5). A feature is `done` only after all 5 phases pass for every task and its `spec.md` reflects reality.