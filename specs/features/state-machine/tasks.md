# Tasks — 3. LangGraph State Machine

Phase gates per AGENTS.md: 1 socratic-architect → 2 ponytail → 3 implementation → 4 adversarial-reviewer → 5 feynman. Task state: **todo → in-progress → done → blocked**. Update the board (`specs/tasks.md`) after each verified cycle.

| # | Task | Phases 1–5 | Verified |
|---|---|---|---|
| 3.1 | StateGraph skeleton + states/edges | | |
| 3.2 | SLM classifier stage (Ollama local) outputting confidence + ambiguous | | |
| 3.3 | Escalation rule + unit tests (D-4) | | |
| 3.4 | MCP client stage calling feature-2 server | | |
| 3.5 | RCA stage → strict-JSON plan + fail-loud schema check | | |
| 3.6 | Gate + MemorySaver checkpointer + decision resume (§5.4) | | |
| 3.7 | `manual_review` path test (AI-down fixture) | | |
| 3.8 | Update board (`specs/tasks.md`) | | |

Context7 stamps go here before any external-library code (AGENTS.md §2).